export CLICOLOR=1
export LSCOLORS=ExFxCxDxBxegedabagacad

# [[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile

# Terminal opens a login shell. This means
# ~/.bash_profile will get executed, ~/.bashrc not
# Solution is to require ~/.bashrc in ~/.bash_profile
# [[ -s ~/.bashrc ]] && source ~/.bashrc
